import { BoundedRangePipe } from './bounded-range.pipe';

describe('BoundedRangePipe', () => {
  it('create an instance', () => {
    const pipe = new BoundedRangePipe();
    expect(pipe).toBeTruthy();
  });
});
