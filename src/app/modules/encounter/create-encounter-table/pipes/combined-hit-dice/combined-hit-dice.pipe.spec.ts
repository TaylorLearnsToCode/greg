import { CombinedHitDicePipe } from './combined-hit-dice.pipe';

describe('CombinedHitDicePipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedHitDicePipe();
    expect(pipe).toBeTruthy();
  });
});
