import { EncounterRollPipe } from './encounter-roll.pipe';

describe('EncounterRollPipe', () => {
  it('create an instance', () => {
    const pipe = new EncounterRollPipe();
    expect(pipe).toBeTruthy();
  });
});
