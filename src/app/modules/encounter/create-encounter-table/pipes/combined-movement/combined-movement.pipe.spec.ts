import { CombinedMovementPipe } from './combined-movement.pipe';

describe('CombinedMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new CombinedMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
