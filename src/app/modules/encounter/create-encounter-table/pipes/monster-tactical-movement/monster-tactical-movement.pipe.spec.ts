import { MonsterTacticalMovementPipe } from './monster-tactical-movement.pipe';

describe('MonsterTacticalMovementPipe', () => {
  it('create an instance', () => {
    const pipe = new MonsterTacticalMovementPipe();
    expect(pipe).toBeTruthy();
  });
});
