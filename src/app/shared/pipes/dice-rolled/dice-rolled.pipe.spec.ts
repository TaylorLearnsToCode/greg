import { DiceRolledPipe } from './dice-rolled.pipe';

describe('DiceRolledPipe', () => {
  it('create an instance', () => {
    const pipe = new DiceRolledPipe();
    expect(pipe).toBeTruthy();
  });
});
