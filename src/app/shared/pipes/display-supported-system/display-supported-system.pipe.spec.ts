import { DisplaySupportedSystemPipe } from './display-supported-system.pipe';

describe('DisplaySupportedSystemPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySupportedSystemPipe();
    expect(pipe).toBeTruthy();
  });
});
