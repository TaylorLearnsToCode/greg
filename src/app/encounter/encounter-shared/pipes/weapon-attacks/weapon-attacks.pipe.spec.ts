import { WeaponAttacksPipe } from './weapon-attacks.pipe';

describe('WeaponAttacksPipe', () => {
  it('create an instance', () => {
    const pipe = new WeaponAttacksPipe();
    expect(pipe).toBeTruthy();
  });
});
