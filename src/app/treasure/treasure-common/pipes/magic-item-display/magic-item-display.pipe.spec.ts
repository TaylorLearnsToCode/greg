import { MagicItemDisplayPipe } from './magic-item-display.pipe';

describe('MagicItemDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new MagicItemDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
