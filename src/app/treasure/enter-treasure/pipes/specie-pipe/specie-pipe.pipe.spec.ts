import { SpeciePipePipe } from '@treasure/enter-treasure/pipes/specie-pipe/specie-pipe.pipe';

describe('SpeciePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SpeciePipePipe();
    expect(pipe).toBeTruthy();
  });
});
