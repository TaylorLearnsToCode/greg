import { GemOrJewelPipe } from './gem-or-jewel.pipe';

describe('GemOrJewelPipe', () => {
  it('create an instance', () => {
    const pipe = new GemOrJewelPipe();
    expect(pipe).toBeTruthy();
  });
});
